/**
 * Thème sémantique ERP — Rouge / Bleu / Vert
 *
 * Bleu  → actions principales, liens, navigation, brand
 * Rouge → erreurs, danger, suppression, déconnexion
 * Vert  → succès, validation, statuts positifs
 */
export const themeColors = {
  primary: 'primary',       // bleu
  destructive: 'destructive', // rouge
  success: 'success',       // vert
}
